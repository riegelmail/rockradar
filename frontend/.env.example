# Base URL of the RockRadar backend API. If unset, the app falls back to
# the production Render URL, so this is only needed for local dev against
# a different backend (e.g. localhost) or a non-default deployment.
VITE_API_BASE=https://rockradar-backend.onrender.com
